services:
  - type: web
    name: sistema-vendas
    runtime: java
    plan: free
    branch: feature/v2-spring-boot
    buildCommand: ./mvnw clean install -DskipTests
    startCommand: java -jar target/sistema-vendas-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SUPABASE_DB_PASSWORD
        sync: false
